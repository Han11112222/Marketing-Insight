# Core
streamlit>=1.33
pandas>=2.2.0
pyarrow>=16.0.0
plotly>=5.22.0
numpy>=1.26.0

# Optional: 히트맵 클릭 선택(설치 시 탭2에서 클릭으로 업종/기간 선택 가능)
# streamlit-plotly-events>=0.0.6

# Optional: 엑셀(xlsx) 파일 직접 읽기
# openpyxl==3.1.5
